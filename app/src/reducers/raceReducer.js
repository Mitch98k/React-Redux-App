
const initialState = {
    races: null,
    isFetching: false,
    error: ''
};

const raceReducer = (state = initialState, action) => {
    switch (action.type) {
        default: return state;
    }
};

export default raceReducer;